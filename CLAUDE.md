# CLAUDE.md

## Agent skills

### Issue tracker

GitHub Issues in mhuang74/googleads-rs via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Three-label lifecycle (ready-for-agent, in-progress, api-upgrade-failed) plus the `api upgrade bot` marker, per ADR 0001. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` glossary at repo root, `docs/adr/` for decision records. See `docs/agents/domain.md`.